class BaseStation:
    def __init__(self, longitude, latitude):
        # Initialize a BaseStation object with longitude and latitude coordinates
        self.longitude = longitude
        self.latitude = latitude
